# Generic runner for the notifier / schema / generator jars. Build arg picks the jar.
FROM eclipse-temurin:21-jre-jammy
ARG JAR
COPY ${JAR} /app/app.jar
WORKDIR /app
ENTRYPOINT ["java", "-jar", "/app/app.jar"]
